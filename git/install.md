## 🐱 Git

## Установка Git

### Ubuntu

Для версии 20.04

как правило сначала нужно выполнить

```sh
sudo apt-get update
```

а затем уже
```sh
sudo apt-get install -y git
```